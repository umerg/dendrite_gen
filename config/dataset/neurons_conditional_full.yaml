name: neurons_conditional_full_dataset
# Cleaned, class-labelled neurons (soma-rooted, binarized, radii kept) with NO
# root-degree cap: the model one-hot width MAX_CHILDREN=23 = max primary-dendrite
# count observed in the corpus, so no neuron is filtered by soma degree (26,469
# kept vs 26,445 for the old cap=16 set). Produced by
# preprocessing/prepare_conditional_dataset.py. Local staging path is
# /Users/umer/Documents/neurons_conditional_full; set data_dir to the rsync'd cluster copy.
data_dir: /scratch/guptau/neurons_conditional_full
load: True
pos_scale_factor: 45.1
